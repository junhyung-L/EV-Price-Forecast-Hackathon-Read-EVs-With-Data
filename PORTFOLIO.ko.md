# 중고 전기차 가격 예측: 배터리 결측치를 별도 문제로 다룬 XGBoost 회귀

[English](PORTFOLIO.md) | [한국어](PORTFOLIO.ko.md)

## 프로젝트 개요

중고 전기차 가격은 연식과 주행거리만으로 설명하기 어렵다. 제조사·모델·차량 상태에 따라 감가 패턴이 달라지고, 가격에 직접적인 영향을 줄 수 있는 배터리 용량도 일부 행에서 비어 있다. 이 프로젝트는 DACON 대회 데이터를 대상으로 가격을 예측하면서, 배터리 결측을 단순 평균으로 대체하지 않고 별도의 예측 문제로 풀었다.

## 핵심 판단

가장 중요한 판단은 배터리 용량을 가격 모델의 전처리 부속 작업으로 취급하지 않은 점이다. 배터리 용량은 제조사, 모델, 차량 상태, 구동 방식, 주행거리, 보증 기간과 연결되어 있다. 따라서 관측된 배터리 용량을 가진 학습 행만으로 XGBoost 대체 모델을 먼저 학습하고, 그 모델을 검증·테스트 행의 결측치에 적용했다. 이 순서는 검증 데이터의 정보를 대체 모델 학습에 섞지 않기 위한 것이다.

가격 모델에는 제조사·모델·차량 상태의 결합 피처, 주행거리 대비 연식 비율, 전비(km/kWh), 중고 여부, 구동 방식과 사고 이력 가중치를 넣었다. 연식이 같은 차량이라도 운행 강도와 배터리 효율이 다를 수 있다는 가정에서 출발한 피처 설계다. 범주형 값은 one-hot encoding, 수치형 결측은 median 대체로 처리하고 XGBoost 회귀 모델로 최종 가격을 예측했다.

## 검증과 결과

유지보수 중인 CLI는 seed 42의 80/20 무작위 holdout으로 RMSE를 계산하고, 전체 학습 데이터로 다시 학습해 제출 CSV를 만든다. 사용자 확인 DACON 정규화 리더보드 RMSE는 **0.919**다. 이 값은 실제 대회 제출 결과로 포트폴리오에 유지한다.

다만 이 저장소에는 당시 `train.csv`, `test.csv`, 제출 파일, 리더보드 캡처가 남아 있지 않다. 따라서 로컬에서 새로 산출되는 holdout RMSE는 0.919와 같은 척도라고 볼 수 없다. 대회 리더보드와 로컬 holdout은 데이터 분할과 평가 환경이 다르기 때문이다.

## 이 프로젝트가 보여주는 역량

이 프로젝트는 결측치를 평균으로 채운 뒤 모델을 돌리는 방식에서 벗어난다. 결측치가 어떤 변수와 연관되는지 먼저 보고, 대체 모델과 가격 모델을 분리해 구성했다. 또한 대회용 노트북에서 끝내지 않고, 입력·출력 경로와 seed를 정리한 실행 스크립트로 바꿔 재실행 가능한 뼈대를 남겼다.

## 한계와 다음 단계

현재 검증은 단일 무작위 분할이므로 특정 연식·제조사·시장 시점으로의 일반화를 보장하지 않는다. 다음 단계에서는 원본 제출물과 데이터 버전을 보관하고, 평균 대체·모델 기반 대체·결측 플래그 방식의 RMSE를 같은 분할에서 비교할 필요가 있다.

## 근거

- 실행 파이프라인: [`src/ev_price_prediction_xgb.py`](src/ev_price_prediction_xgb.py)
- 원본 실험 노트북: [`notebooks/EV_price_prediction_xgb.ipynb`](notebooks/EV_price_prediction_xgb.ipynb)
- 프로젝트 리뷰: [`docs/PROJECT_REVIEW.md`](docs/PROJECT_REVIEW.md)

## 결측 처리를 별도 모델로 둔 이유

배터리 용량은 전기차 가격에서 단순한 보조 컬럼이 아니다. 같은 연식·주행거리라도 제조사와 모델, 구동 방식, 차량 상태, 보증 기간이 다르면 배터리 사양과 잔존 가치의 관계도 달라진다. 그래서 관측된 배터리 용량 행만으로 대체 모델을 학습하고, 그 모델을 결측 행에 적용한 뒤 가격 모델로 넘겼다. 가격을 먼저 보고 결측치를 채우거나 검증 행을 대체 모델 학습에 포함하는 누수를 피하기 위한 순서다.

최종 회귀에는 제조사·모델·상태의 결합, 주행거리 대비 연식, 전비(km/kWh), 중고 여부, 구동 방식, 사고 이력 가중치를 넣었다. 이 피처들은 ‘주행거리가 많을수록 싸다’ 같은 한 방향 규칙을 넘어 차량 조합별 감가 차이를 표현하려는 시도다. 범주형 값은 one-hot encoding, 일반 수치 결측은 중앙값 대체로 처리했다.

사용자가 확인한 DACON 정규화 리더보드 RMSE **0.919**는 실제 제출 성적이므로 그대로 포트폴리오에 둔다. 다만 현재 체크아웃에는 제출 파일과 당시 원본 데이터가 없어서, CLI의 80/20 holdout 결과를 같은 숫자로 비교하면 안 된다. 다음에는 대회 제출 CSV, 데이터 버전, 특성 목록, 대체 모델과 가격 모델의 분할을 함께 보관해 0.919가 나온 정확한 실험 조건까지 추적 가능하게 만들 계획이다.
